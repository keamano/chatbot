package com.example.service;

import java.util.List;

import com.example.entity.Bot;
import com.example.entity.Chat;

public interface ChatService {

    List<Chat> findAll();

    void insert(Chat bot);
    
    void update(Chat bot);
    
    void delete(Chat bot);
}
